<div class="container">
  <%= render "shared/searchbar" %>
</div>
<div class="main-container">
  <div class="index-cards">
    <% @machines.each do |machine| %>  
      <div class="card-details">
        <%= link_to machine_path(machine) do %>
          <% if machine.photo.attached? %>
            <%= cl_image_tag machine.photo.key %>
          <% end  %>
          <div class="card-text-elements">
            <div class="card-title-description">
              <h2><%= link_to machine.name, machine_path(machine) %></h2>
            </div>
            <div class="card-icons">
              <div>
                <p>💰</p>
                <h3><%= machine.price %>€/ jour</h3>
              </div>
              <div>
                <p>🍽️</p>
                <h3><%= machine.capacity %></h3>
              </div>
            </div>
          </div>
        <% end %>
      </div>
    <% end %>
  </div>
  <div id="map"
      style="width: 100%; height: 600px;"
      data-markers="<%= @markers.to_json %>"
      data-mapbox-api-key="<%= ENV['MAPBOX_API_KEY'] %>">
  </div>
</div>
